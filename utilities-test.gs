function myFunction() {
  
  
  /* JAVASCRIPT customer alert
          Swal.fire({
            title: 'Select Outage Tier',
            html: '<select id="alcType" class="" title="Please select..." name="alcType"><option>one</option></select>',
            showCancelButton: true,
            preConfirm: function() {
            return new Promise((resolve, reject) => {
                    // get your inputs using their placeholder or maybe add IDs to them
                    resolve({
                        Option: $('#supsup').val(),
                    });
    
                    // maybe also reject() on some condition
                });
            },
            inputValidator: function (value) {
              return new Promise(function (resolve, reject) {
                if (value !== '') {
                  resolve();
                } else {
                  resolve('You need to select a Tier');
                }
              });
            }
          }).then((result) => {
            if (result.value) {
             console.log(result);
            } else {
              Swal.fire({
                type: 'info',
                html: 'Close call!'
              });
            }
          });
          
          */
}
